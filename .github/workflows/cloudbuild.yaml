steps:
  - id: 'Deploy DAGs to GCS'
    name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud storage cp -r airflow/dags/* gs://$_BUCKET_NAME/

options:
  logging: CLOUD_LOGGING_ONLY